# -x
# set -x
# set +x
# -e = Exit aon error
# Can be combined with other options
#!/bin/bash -ex or -xe
# -v = Prints shell input lines as they are read. You can use it for seeing substitution and expansions
# help set | less
